<h1><%= @item.title %></h1>
<h2><%= number_to_currency(@item.price.to_f/100) %></h2>
<h3><%= @item.inventory %> left</h3>
<p><%= button_to 'Add to Cart', line_items_path(item_id: @item) if user_signed_in? %></p>
